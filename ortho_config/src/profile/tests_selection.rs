//! Tests for selection resolution: flag beats environment, empty means
//! unset (milestone 3).

use pretty_assertions::assert_eq;

#[test]
fn verifies_pretty_assertions_is_usable() {
    assert_eq!(40 + 2, 42);
}
