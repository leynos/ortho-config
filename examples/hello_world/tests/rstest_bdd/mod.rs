//! `rstest-bdd` scaffolding for the `hello_world` example crate.
//!
//! The modules provide fixtures, steps, and a canary scenario so behavioural
//! coverage can run under the stock `cargo test` harness.

mod fixtures;
mod steps;
mod canary;
